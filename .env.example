# Copy this file to .env and replace the local-only placeholder passwords.
MYSQL_DATABASE=yourhome
MYSQL_USER=yourhome
MYSQL_PASSWORD=replace-with-a-local-password
MYSQL_ROOT_PASSWORD=replace-with-a-local-root-password

# WordPress is the only service published to the host.
APP_PORT=8080
